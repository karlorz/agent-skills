interface:
  display_name: "Autopilot"
  short_description: "Managed Codex home-hook autopilot"
  default_prompt: "Use $autopilot to install or refresh the generic managed Codex home-hook autopilot flow, with repo-local `.codex/hooks/*.sh` overrides only when this workspace needs custom routing."
