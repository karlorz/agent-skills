{
  "mcpServers": {
    "grok-search": {
      "type": "http",
      "url": "${GROK_SEARCH_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${GROK_SEARCH_MCP_TOKEN}"
      }
    }
  }
}
