{
  "mcpServers": {
    "grok-search": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/scripts/run-grok-search.sh"
      ],
      "env": {
        "GUDA_API_KEY": "${GUDA_API_KEY}",
        "GUDA_BASE_URL": "${GUDA_BASE_URL}"
      }
    }
  }
}
