interface:
  display_name: "Simplify"
  short_description: "Review and simplify recently changed code for reuse, simplification, efficiency, and altitude while preserving behavior. Use when the user asks to simplify, refine, polish, clean up, or make code clearer, or after finishing a logical chunk of implementation that should be tightened before commit."
  default_prompt: "Use $simplify to gather the current diff, run reuse, simplification, efficiency, and altitude review, then apply safe fixes while preserving behavior."
